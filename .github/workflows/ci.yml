# ── CI Workflow ───────────────────────────────────────────────────────────────
# This is a placeholder. Fill in the TODO steps below for your project's stack.
# Rename or delete this comment block when you're done.
# ─────────────────────────────────────────────────────────────────────────────

name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # TODO: Set up your language runtime
      # Examples:
      # - uses: actions/setup-node@v4
      #   with: { node-version: "lts/*" }
      #
      # - uses: actions/setup-python@v5
      #   with: { python-version: "3.x" }
      #
      # - uses: actions/setup-go@v5
      #   with: { go-version: "stable" }

      # TODO: Install dependencies
      # - run: npm ci
      # - run: pip install -e '.[dev]'
      # - run: go mod download

      # TODO: Run linters
      # - run: npm run lint
      # - run: ruff check .

      # TODO: Run tests
      # - run: npm test
      # - run: pytest
      # - run: go test ./...

      - name: Placeholder — remove this step once the TODOs above are filled in
        run: echo "CI is not configured yet. See the TODO comments above."
