# YAML linting rules.
# Full reference: https://yamllint.readthedocs.io/en/stable/rules.html

extends: default

rules:
  line-length:
    max: 120
    level: warning
  truthy:
    allowed-values: ["true", "false", "on", "off"]
    check-keys: false
  comments:
    min-spaces-from-content: 1
